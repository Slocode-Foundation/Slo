mod lexer;
use lexer::tokenize;

fn main() {
    // Yes. there is nothing here. here is some test code to mess with.
    // fib: Int -> Int
    // fib(0) = 0
    // fib(1) = 1
    // fib(n) = fib(n-1) + fib(n-2)

    // or "fib: Int -> Int\nfib(0) = 0\nfib(1) = 1\nfib(n) = fib(n-1) + fib(n-2)";
}